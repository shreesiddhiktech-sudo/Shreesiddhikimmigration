const db = require("../models");
const { getVisaCategories } = require("./visacategories.controller");
it db.AgentVisaFile.create({
      file_number,
      name,
      email,
      visa_category_id,
      country_id,
      agent_id,
    });

    return res.status(200).json({
      status: 0,
      mes
    return res.status(500).json({ status: 1, message: err.message });
  }
};
