module.exports = (sequelize, DataTypes) => {
  return sequelize.define(
    "VisaCategory",
    {
      id: {
    
        typ
      tableName: "visa_categories",
      timestamps: true,
    }
  );
};
